class Math {
	add = (a, b) => {return a + b;}
	subtract = (a, b) => {return a - b;}
	divide = (a, b) => {return a / b;}
	multiply = (a, b) => {return a * b;}
}
export const { add, subtract, divide, multiply } = new Math();

